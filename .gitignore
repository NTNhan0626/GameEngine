# File cấu hình môi trường (nếu có)
.env
.env.local
.env.*.local

# Node modules (thư viện bên ngoài)
node_modules/

# File lock (tùy chọn - có thể giữ lại nếu dùng npm ci)
package-lock.json
yarn.lock
pnpm-lock.yaml

# MacOS hệ điều hành
.DS_Store

# VSCode settings (nếu không muốn sync)
.vscode/

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Output tsc nếu dùng tsconfig khác
*.tsbuildinfo